// Before
export const metadata: Metadata = {
  // ... other metadata
  viewport: "width=device-width, initial-scale=1, maximum-scale=1",
};

// After - Separated viewport configuration
export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
};

// Before
<html lang="en">
  <body className="child-mode" suppressHydrationWarning={true}>

// After - Added suppressHydrationWarning to both html and body
<html lang="en" suppressHydrationWarning>
  <body className="child-mode" suppressHydrationWarning>